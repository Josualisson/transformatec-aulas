/* function gritar(expr){   
    //expr = !!!!!!!!
    return expr + '!!!!!!!!'
}

const x = gritar('bom dia')
console.log(x) */


/* // função com parâmetro
function soma(a, b) {
    //return é usado pra dizer o que vai ser aquele parâmetro
    return a + b;
}
// criando uma variável pra receber os valores do parâmetro
const resultado = soma(1, 2);
console.log(resultado) */

//função anônima que não precisa de nome

const multiplicacao = function (a, b){
    return a - b;
}
console.log(multiplicacao(5,1))
